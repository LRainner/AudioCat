{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "preference"],
  "permissions": [
    "core:default",
    "opener:default",
    "fs:allow-exists",
    "fs:allow-read-file",
    "fs:allow-write-file",
    "fs:allow-appdata-read",
    "fs:allow-appdata-write"
  ]
}
